"""NativeMoveStrategy / FortranMoveStrategy.

STATUS: placeholder.

Source material for this migration:
- macroscale.py lines ~1000-1100 and ~1300-1400 (the `if duplicate_fortran`
  branches for move-probability check and neighbor selection)

from .base import MoveStrategy
"""
